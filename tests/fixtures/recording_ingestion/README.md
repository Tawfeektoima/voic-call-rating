Synthetic recording-ingestion fixtures used by tests.

The `valid_tiny_audio.wav` and `valid_tiny_audio.mp3` files are real, tiny
audio samples generated locally for parser and signature checks. The remaining
binary/text fixtures are intentionally synthetic or malformed so tests can
exercise failure paths without contacting external services.
